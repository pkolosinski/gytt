# 0003 — Version habit definitions

Accepted. Each Habit will retain immutable, effective-dated definition versions, while progress is stored against a deterministic Habit occurrence identity. This preserves closed-period history and permits a new definition to replace the current weekly or monthly occurrence without copying all occurrences into storage.
